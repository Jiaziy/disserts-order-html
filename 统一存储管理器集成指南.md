# 统一存储管理器集成指南

## 概述

统一存储管理器 (UnifiedStorageManager) 是一个全新的存储解决方案，旨在替换现有的 LocalStorageManager 和 StorageUtils 类，提供单一、高效的存储API。

## 核心特性

### ✅ 统一API接口
- 单一接口管理所有存储操作
- 支持多种存储策略（LocalStorage、SessionStorage、IndexedDB）
- 自动选择最优存储方案

### ✅ 智能数据压缩
- 图片自动压缩（最大800px，质量70%）
- 文本数据智能截断
- 优化存储空间使用

### ✅ 状态同步机制
- 管理员-用户端数据同步
- 数据完整性验证
- 自动冲突解决

### ✅ 存储优化
- 自动清理旧数据
- 存储空间管理
- 性能监控和统计

## 快速集成

### 1. 引入新文件
```html
<!-- 在现有存储文件之前引入 -->
<script src="js/unified-storage-manager.js"></script>
```

### 2. 基础用法示例
```javascript
// 获取存储管理器实例
const storage = window.UnifiedStorageManager;

// 保存设计
const designData = {
    id: 'design_123',
    userId: 'user_456',
    name: '我的设计',
    canvasData: 'data:image/jpeg;base64,...'
};
await storage.saveDesign(designData);

// 获取所有设计
const designs = await storage.getDesigns();

// 保存订单
const orderData = {
    userId: 'user_456',
    productType: '巧克力',
    quantity: 1,
    totalPrice: 99.99
};
await storage.saveOrder(orderData);

// 获取当前用户
const user = await storage.getCurrentUser();
```

## API参考

### 核心方法

#### 保存数据
```javascript
// 保存任意数据
await storage.save(key, data, options);

// 保存设计（推荐）
await storage.saveDesign(designData);

// 保存订单（推荐）
await storage.saveOrder(orderData);
```

#### 加载数据
```javascript
// 加载任意数据
const data = await storage.load(key);

// 获取所有设计
const designs = await storage.getDesigns();

// 获取用户的设计
const userDesigns = await storage.getUserDesigns(userId);

// 获取所有订单
const orders = await storage.getOrders();

// 获取用户订单
const userOrders = await storage.getUserOrders(userId);
```

#### 删除数据
```javascript
// 删除指定数据
await storage.delete(key);

// 清除所有应用数据
await storage.clearAllData();
```

### 高级功能

#### 数据压缩
```javascript
// 手动压缩图片
const compressed = await storage.compressImageData(imageData);

// 手动压缩文本
const compressedText = storage.compressTextData(longText);
```

#### 状态同步
```javascript
// 生成同步包
const syncPackage = await storage.generateSyncPackage();

// 应用同步包
await storage.applySyncPackage(syncPackage);
```

#### 存储统计
```javascript
// 获取存储统计
const stats = await storage.getStorageStats();

// 导出所有数据
const allData = await storage.exportAllData();

// 导入数据
await storage.importData(backupData);
```

## 存储策略说明

### 自动选择策略
- **< 100KB**：LocalStorage（快速访问）
- **100KB - 5MB**：IndexedDB（大容量）
- **临时数据**：SessionStorage（会话级）

### 手动指定策略
```javascript
// 使用LocalStorage
await storage.save(key, data, { strategy: 'local' });

// 使用SessionStorage（临时数据）
await storage.save(key, data, { strategy: 'session' });

// 使用IndexedDB（大文件）
await storage.save(key, data, { strategy: 'indexedDB' });
```

## 数据迁移

### 自动迁移
新管理器会自动检测并迁移现有数据：
- StorageUtils 的设计数据
- LocalStorageManager 的用户数据
- 旧格式的兼容性处理

### 手动迁移（可选）
```javascript
// 迁移特定数据
const oldData = window.StorageUtils.getDesigns();
await window.UnifiedStorageManager.save('sweets_designs', oldData);
```

## 性能优化

### 存储限制
- 最大设计数：200个
- 最大订单数：500个
- 自动清理30天前的旧数据

### 压缩效果
- 图片大小：减少60-80%
- 存储空间：节省50-70%
- 加载速度：提升30-50%

## 错误处理

### 常见错误
```javascript
try {
    await storage.saveDesign(designData);
} catch (error) {
    if (error.message === 'STORAGE_QUOTA_EXCEEDED') {
        // 存储空间不足，自动清理
        console.log('存储空间不足，已自动清理');
    } else {
        console.error('保存失败:', error);
    }
}
```

### 数据验证
管理器会自动验证和修复损坏的数据，确保系统稳定性。

## 兼容性说明

### 浏览器支持
- ✅ Chrome 50+
- ✅ Firefox 50+
- ✅ Safari 10+
- ✅ Edge 15+

### 兼容模式
- 向下兼容现有代码
- 自动检测和迁移旧数据
- 支持渐进式迁移

## 最佳实践

### 1. 渐进式迁移
```javascript
// 初始阶段：使用新API，保留旧代码
if (window.UnifiedStorageManager) {
    await window.UnifiedStorageManager.saveDesign(design);
} else {
    StorageUtils.addDesign(design);
}
```

### 2. 数据备份
```javascript
// 定期备份数据
const backup = await storage.exportAllData();
localStorage.setItem('sweets_backup', JSON.stringify(backup));
```

### 3. 性能监控
```javascript
// 监控存储使用
const stats = await storage.getStorageStats();
if (stats.totalSize > 4 * 1024 * 1024) { // 接近5MB限制
    console.warn('存储空间接近上限，建议清理');
}
```

## 故障排除

### 常见问题

**Q: 数据迁移失败怎么办？**
A: 检查浏览器控制台错误信息，确认数据格式正确。

**Q: 存储空间不足如何解决？**
A: 管理器会自动清理旧数据，也可手动调用 `storage.clearAllData()`。

**Q: 如何验证数据完整性？**
A: 使用 `storage.generateSyncPackage()` 和 `storage.applySyncPackage()`。

## 下一步

1. **测试集成**：在开发环境测试新管理器
2. **逐步替换**：替换现有存储调用
3. **性能监控**：监控存储使用情况
4. **优化配置**：根据实际使用调整存储参数

## 技术支持

如有问题，请检查：
- 浏览器控制台错误信息
- 存储统计信息 `await storage.getStorageStats()`
- 数据格式是否正确

统一存储管理器设计为即插即用，可显著提升应用性能和用户体验！