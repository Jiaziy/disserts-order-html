# 设计器调试指南

## 🔧 问题诊断步骤

### 1. 打开调试页面
访问 `debug-designer.html` 页面进行系统诊断

### 2. 初始化存储管理器
1. 点击 **"初始化存储管理器"** 按钮
2. 等待 2 秒让异步初始化完成
3. 检查是否显示成功消息

### 3. 检查依赖项
1. 点击 **"检查依赖项"** 按钮
2. 确保所有项目都显示 ✅
3. 如果有 ❌，查看具体错误信息

### 4. 测试存储功能
1. 点击 **"测试存储功能"** 按钮
2. 观察保存和读取测试是否成功
3. 检查是否有配额错误

### 5. 检查设计器状态
1. 打开 `sweets-designer.html` 页面
2. 切换到调试页面窗口
3. 点击 **"检查设计器状态"** 按钮

## 🔍 常见问题解决

### 问题: `unifiedStorageManager 实例: ❌ 未初始化`
**原因**: 统一存储管理器没有正确初始化
**解决方法**:
1. 点击"初始化存储管理器"按钮
2. 等待 2 秒后再次检查依赖项
3. 如果仍失败，检查控制台错误信息

### 问题: 存储配额超限错误
**原因**: 浏览器本地存储空间不足
**解决方法**:
1. 访问 `storage-cleanup-tool.html`
2. 点击"执行存储清理"
3. 或者手动清理浏览器数据

### 问题: 保存设计时出现错误
**原因**: 渲染器或画布未正确初始化
**解决方法**:
1. 在设计器页面刷新页面
2. 确保页面完全加载后再尝试保存
3. 检查控制台是否有初始化错误

## 📋 控制台诊断信息

在设计器页面 (`sweets-designer.html`) 按以下步骤查看详细信息：

1. **打开开发者工具**:
   - 按 F12 或右键选择"检查"
   - 切换到"控制台"标签

2. **查看初始化日志**:
   - 查找 "=== 存储设置诊断 ===" 
   - 确认统一存储管理器状态
   - 检查存储键和大小信息

3. **查看保存过程日志**:
   - 点击"保存设计"按钮
   - 查看详细的保存过程日志
   - 定位具体的错误位置

## 🚨 错误信息解读

### `QuotaExceededError`
- **含义**: 存储配额超限
- **解决**: 使用存储清理工具清理旧数据

### `渲染器未初始化`
- **含义**: 渲染器模块没有正确加载
- **解决**: 检查脚本加载顺序，刷新页面

### `画布数据为空`
- **含义**: 尝试保存空白画布
- **解决**: 先进行一些绘制操作再保存

### `统一存储管理器未加载`
- **含义**: 统一存储管理器脚本加载失败
- **解决**: 检查 `unified-storage-manager.js` 文件是否存在

## 🛠 手动修复步骤

如果自动修复无效，可以尝试以下手动步骤：

### 1. 重置存储空间
```javascript
// 在浏览器控制台中执行
localStorage.clear();
location.reload();
```

### 2. 手动初始化存储管理器
```javascript
// 在设计器页面控制台中执行
window.unifiedStorageManager = new UnifiedStorageManager();
```

### 3. 强制清理重复数据
```javascript
// 在控制台中执行
const keys = ['sweetsDesigns', 'sweets_designs', 'designs'];
keys.forEach(key => localStorage.removeItem(key));
location.reload();
```

## 📞 获取帮助

如果以上方法都无法解决问题：

1. **收集错误信息**:
   - 控制台中的错误堆栈
   - 调试页面的诊断结果
   - 具体的操作步骤

2. **检查浏览器兼容性**:
   - 确保使用现代浏览器
   - 禁用过于严格的隐私设置
   - 允许本地存储访问

3. **尝试无痕模式**:
   - 在无痕/隐私模式下测试
   - 排除浏览器扩展的干扰

按照这个指南逐步排查，应该能够定位并解决大部分设计器相关的问题。